#Create a function that combines the functions we learned today (lower(), upper(), capitalize(), find())
def list(listn,replace):
    listn[x] = "Apple"
    return listn
x = int(input("Enter which you want to replace: "))
print(list(["Banana", "fruit","home","clock"], x))